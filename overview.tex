\section{Overview}
\label{sec:overview}

% We start with an informal warm up session before moving on to the formal development.
% The main insight behind the technique is that:
% Symbolic execution already computes most of the structure needed for a proof, path conditions become hypotheses, and execution steps correspond to semantic rules.
% The contribution of the paper is to make this correspondence explicit and mechanized, yielding a system where:
% - symbolic execution is used for automation
% - proof checking is used for trust

In this section,
we give an informal overview of our approach before moving on to the formal development (\Cref{sec:program-safety}).
For the purpose of this overview,
we assume a standard \emph{formal} and \emph{executable} symbolic semantics, without relying on specific implementations.
The \emph{formal} symbolic semantics are those explicitly mechanized inside the proof assistant (\eg \coq).
The \emph{executable} symbolic semantics are those implicitly induced by the implementation of the \SE engine (\eg \klee).
In general,
those semantics are not necessarily identical,
since \SE engines employs various encoding optimizations which may not be fully modeled by mechanized semantics.

\begin{figure}
\begin{minipage}[t]{0.55\textwidth}
\vspace{3pt}
\lstinputlisting[basicstyle=\scriptsize\ttfamily,language=llvm,mathescape=true,breaklines=false]{code/overview.simple.ll}
\vspace{20pt}
\lstinputlisting[basicstyle=\scriptsize\ttfamily,language=c,mathescape=true,breaklines=false]{code/overview.simple.c}
\end{minipage}
\begin{minipage}[t]{0.40\textwidth}
\vspace*{0pt}
\includegraphics[width=\textwidth]{images/tree.pdf}
\end{minipage}
\caption{A simple \llvm IR program (top left), its C source code (bottom left), and its execution tree (right).}
\label{figure:overview-llvm}
\end{figure}

\subsection{Running Example}

Consider the \llvm IR program in~\Cref{figure:overview-llvm},
and suppose that we want to prove that this program is \emph{safe},
\ie it does not trigger any errors for valid inputs.
The program allocates a heap object using \code{malloc} (\cref{line:overview-llvm-malloc}), and then acesses it at offset \code{n} (\cref{line:overview-llvm-store}).
Here, we assume that the input \code{n} satisfies the precondition $0 \leq \code{n} < 10$.

% what happens with a standard SE engine?
A standard \SE engine will analyze this program by executing it with symbolic values rather than concrete ones.
The analysis is carried by tracking symbolic states that abstractly represent the program environment.
Each symbolic state consists of a program counter, a symbolic store for registers, a symbolic memory, and path constraints accumulated along the execution path.
In this example,
it will model the input \code{n} as a symbolic value $n$, while adding the precondition $n < 10$ to the path constraints.
At~\cref{line:overview-llvm-malloc},
it will allocate a heap object of size 10.
At~\cref{line:overview-llvm-gep},
it will compute the pointer \code{p} to be accessed.
At~\cref{line:overview-llvm-store},
it will store the value \code{7} into \code{p}.
To do so,
it will first check whether this access may be out-of-bounds,
by checking using the SMT solver the satisfiability of the condition $n \geq 10 \lor n < 0$ with respect to the path constraints.
In this case, this condition is unsatisfiable, so this access is guaranteed to be error-free.
Overall, it will explore all the feasible paths without reporting any errors.

% why is it not enoguh?
However, a standard \SE engine is a complex software system.
Even a single bug in its implementation can compromise the integrity of the analysis, leading to undetected errors.
To reduce the reliance on a massive trusted computing base,
our goal is to generate an independent and machine-checked proof ensuring that an exhaustive exploration of the input program indeed guarantees its safety.

% theory
\subsection{Theoretical Framework}

To generate machine-checked safety proofs, we develop a formal framework in \coq.
Our theoretical framework defines the concrete (\Cref{sec:concrete-semantics}) and symbolic (\Cref{sec:symbolic-semantics}) semantics of \llvm.
When the symbolic execution of a given program is \emph{exhaustive},
the explored paths form an \emph{execution tree},
where each node represents a symbolic state computed by the \SE engine.
The main result of our theoretical framework is the \emph{program safety} theorem (\Cref{theorem:safety-via-et}).
Intuitively, this theorem states that if the execution tree of the program satisfies certain \emph{safety properties},
then the program is safe with respect to the concrete \llvm semantics.
More specifically, the required safety properties are \emph{error-freeness} and \emph{completeness} (\Cref{def:safe-et}).
The former requires that the execution tree must not contain error symbolic states.
The latter requires that the execution tree over-approximates all the feasible symbolic states derivable by the symbolic \llvm semantics.
This theorem bridges the concrete and symbolic domains,
providing a foundation for automating proof generation.

% proof generation
\subsection{Proof Generation}

To apply the \emph{program safety} theorem,
we must provide an execution tree that satisfies the safety properties mentioned above.
To do so, we instrument the \SE engine to generate an execution tree along with a proof showing that it satisfies the desired properties.
More specifically,
for every instruction executed by the \SE engine,
we generate a lemma that proves the safety properties of the corresponding subtree.
This way, we decompose the global proof obligation of the entire program into instruction-level proof obligations.

To illustrate the proof generation process, consider again the \llvm IR program in~\Cref{figure:overview-llvm}.
On the right side of~\Cref{figure:overview-llvm},
the yellow nodes correspond to the symbolic states derived by the executable symbolic semantics, \ie those computed by the \SE engine,
and the blue nodes correspond to the symbolic states derived by the mechanized symbolic semantics.
Hence, the execution tree of this program is described by the yellow section of that figure.
%Each node $s_i$ corresponds to a symbolic state,
%and its successors are the symbolic states computed by the \SE engine while executing the current instruction of $s_i$.
Here, for example,
$s_0$ is the initial symbolic state,
$s_1$ is the symbolic state derived from $s_0$ by the executable symbolic semantics,
\ie $s_1$ is computed by the \SE engine when executing the instruction at~\cref{line:overview-llvm-malloc},
and $\bar{s}_1$ is the symbolic state derived from $s_0$ by the mechanized symbolic semantics.

When we execute the instruction of $s_i$,
we generate a lemma stating that the subtree of $s_i$ satisfies the safety properties.
Once the analysis of the whole program is complete,
we compose those lemmas in a bottom-up manner to obtain a lemma stating that the whole execution tree satisfies the safety properties.
In this example,
the lemma of X and Y are generated first, and the lemma of Z is generated only at the end.

To give intuition about how those lemmas are proven, consider the subtree of X.\footnote{
An extended and formal example will be given in~\Cref{sec:proof-generation}.
}
The current instruction at X is the \code{store} instruction at~\cref{line:overview-llvm-store}.

First, we need to prove that this subtree is \emph{error-free}.
More specifically,
we need to prove that X is not an error symbolic state.
In the case of a \code{store} instruction,
we need to show that X cannot point to an out-of-bounds address.
Here, X points to a single object,
the one allocated at~\cref{line:overview-llvm-malloc},
so it will suffice to prove that the constraint X is unsatisfiable.

Second, we need to prove that this subtree is \emph{complete}.
More specifically,
we need to prove that if X can be derived from X by the formal symbolic semantics,
then either X is infeasible or there exist a successor of X in the subtree that is equivalent to X.
In the case of a \code{store} instruction,
the formal symbolic semantics may derive multiple symbolic states,
depending on the number of objects that may be pointed by X.
Here, X points to a single object, so there is only one derived symbolic state.
This state is not infeasible,
so we need to prove that these two symbolic states are equivalent (SMT level).
In general, the formal semantics may differ from the symbolic semantics of the \SE engine,
so the equivalence proof might not be immediate.

% TODO: improve
Lastly, will need to prove that the subtree of X (the successor of X) is safe.
This can be done immediately due to the bottom-up construction of the proof.
